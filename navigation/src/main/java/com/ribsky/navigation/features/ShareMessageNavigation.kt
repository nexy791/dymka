package com.ribsky.navigation.features

import com.ribsky.common.navigation.Navigation

interface ShareMessageNavigation : Navigation {

    companion object {
        const val KEY_SHARE_MESSAGE = "share_key"
        const val KEY_SHARE_MESSAGE_ID = "share_key_id"
    }
}
